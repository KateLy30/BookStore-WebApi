services:
  postgres:
    container_name: postgres
    image: postgres:latest
    environment:
      POSTGRES_DB: "BookStore"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "Admin21K"
    volumes:
      - postgres-data:/var/lib/postgres/data
    ports:
      - "5432:5432"

volumes:
  postgres-data: